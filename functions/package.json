{
  "name": "smartneighborhoodhelper-functions",
  "description": "Cloud Functions for SmartNeighborhoodHelper (FCM v1 push notifications)",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint .",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions,firestore",
    "build": "tsc -p tsconfig.json",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.22.1",
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
