package com.example.smartneighborhoodhelper.ui.adapter

/**
 * ComplaintAdapter.kt — RecyclerView adapter for displaying complaint cards.
 *
 * WHAT IS A RecyclerView Adapter?
 *   RecyclerView is Android's efficient scrolling list. The Adapter tells it:
 *   - How many items to show (getItemCount)
 *   - What each item looks like (onCreateViewHolder → inflates XML layout)
 *   - What data to put in each item (onBindViewHolder → sets text, images, etc.)
 *
 * WILL BE FULLY IMPLEMENTED IN FEATURE 3 (Main Dashboard with Fragments).
 */
// TODO: Implement in Feature 3

