package com.example.smartneighborhoodhelper.viewmodel

/**
 * ComplaintViewModel.kt — ViewModel for complaint reporting, listing, and detail.
 *
 * WILL BE FULLY IMPLEMENTED IN FEATURE 4 (Report Complaint).
 */
class ComplaintViewModel {
    // TODO: Implement in Feature 4
    // - reportComplaint(title, description, category, imageUri, lat, lng)
    // - getComplaints() → LiveData<List<Complaint>>
    // - updateStatus(complaintId, newStatus)
    // - assignProvider(complaintId, providerId)
}

