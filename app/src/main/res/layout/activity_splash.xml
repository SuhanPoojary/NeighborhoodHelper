<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_splash.xml — Splash screen matching Figma design.
    Blue gradient background, rounded icon in center, white text below.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/bg_splash_gradient">

    <!-- Icon container — rounded rectangle with building icon -->
    <FrameLayout
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@drawable/bg_splash_icon"
        android:layout_marginBottom="28dp">

        <ImageView
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_splash_building"
            android:contentDescription="@string/app_name" />

    </FrameLayout>

    <!-- App Name -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Smart Neighborhood\nHelper"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="12dp" />

    <!-- Tagline -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Report • Track • Resolve"
        android:textSize="15sp"
        android:textColor="@color/white"
        android:alpha="0.8" />

</LinearLayout>
